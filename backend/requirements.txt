# ===== Agent Hub V1 后端依赖 =====
# Web 框架
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9  # 文件上传(skill 脚本)

# 配置
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
itsdangerous>=2.2  # 鉴权 token 签名(§7)

# Agent 编排(路线B: deepagents 作运行时主体,构建于 LangGraph 之上)
langgraph>=0.2
langchain-openai>=0.2
deepagents>=0.6  # skills/file_system/subagent harness(§4.6)
langchain-mcp-adapters>=0.1  # MCP 工具支持(§2.1 动作型走 MCP)
mcp>=1.0  # MCP 协议运行时

# 沙箱控制面(§3: Docker 后端)
docker>=7.0

# 数据层(§4.3: PG + pgvector;本轮只连通,暂不建表)
sqlalchemy>=2.0
psycopg2-binary>=2.9
# pgvector 扩展支持(后续知识库 §5.3 用)
pgvector>=0.3

# 气动替身(§4.1: AeroSandbox VLM)
aerosandbox>=4.2
numpy>=1.26

# 知识库文档解析(V2 §3)
PyMuPDF>=1.24  # PDF 文字提取
python-docx>=1.1  # Word(.docx)
openpyxl>=3.1  # Excel(.xlsx)
